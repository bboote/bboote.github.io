# bboote.github.io

Personal homepage, served at https://bboote.github.io

Plain static HTML — no build step. Edit `index.html` and push to `main`.

## Before publishing

- Replace `static/images/me.png` with a real photo (square crop).
- Add `static/cv.pdf`, or repoint the CV link in the header.
- Swap the `.svg` thumbnail placeholders in `static/images/` for real teasers.
- Expand the abbreviated coauthor initials (see the TODO comment in the Research section).
- Fill in real dates in Education and Experience.
